apiVersion: v1
kind: Pod
metadata:
  name: min-pod
  namespace: rq-lab
spec:
  containers:
  - name: nginx-container
    image: nginx:latest
    resources:
      requests:
        cpu: "50m"
        memory: "256Mi"
