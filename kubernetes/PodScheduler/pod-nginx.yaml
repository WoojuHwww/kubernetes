apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod

spec:
  containers:
  - name: nginx-container
    image: nginx:1.29.1
    ports:
      - containerPort: 80
        protocol: TCP
  nodeSelector:
    gpu: "true"
    zone: "lab"
