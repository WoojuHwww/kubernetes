#Ingress와 service를 사용했을 때 html파일이 정상적으로 나오지 않는 오류
- Error Type : 404 

# 문제 원인
- main/auth 컨테이너 생성 시 Ingress.yaml 파일에 설정된 
html 경로를 ingress가 찾을 수 없어서 발생

# 문제 해결
- Ingress rewrite-target 기능을 사용해서 정확한 디렉터리 경로가 아니더라도
상위 디렉터리 경로와 포트 번호가 맞으면 라우팅 될 수 있게 문제 해결

#예시 yaml 파일
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sol-ingress
  namespace: soldesk
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /curriculum(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: curriculum-service
                port:
                  number: 80

          - path: /main(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: main-service
                port:
                  number: 80

          - path: /auth(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: auth-service
                port:
                  number: 80

          - path: /(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: sol-home-service
                port:
                  number: 80



